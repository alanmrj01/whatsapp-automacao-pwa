// Only domains without a public backend API still use these demonstration values.
export const mockDashboard = {
  automationLabel: 'Aguardando conexão',
  appointmentsToday: 0,
  conversationsToday: 0,
}
